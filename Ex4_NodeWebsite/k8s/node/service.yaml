apiVersion: v1
kind: Service

metadata:
  name: node-service
  namespace: devops

spec:
  type: ClusterIP

  selector:
    app: node-app

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000